      {% if user.is_superuser %}
        <p class="card-text price">Â£{{ product.price }}</p>
      {% endif %}
      {% if product.is_bulk %}
      <p class="card-text price">Â£{{ product.rsp }}/item</p>
      <p class="card-text"><small class="text-muted">Pack of {{ product.pack_amount }} â€” Total Â£{{ product.bulk_total_price|floatformat:2 }}</small></p>
      {% else %}
      <p class="card-text price">Â£{{ product.rsp }}</p>
      {% endif %}

